version: '2'
services:
  postgres:
    image: 'postgres:9.5'
    ports:
      - "5432:5432"
    volumes:
      - ./postgres:/var/lib/postgresql/data
  redis:
    image: 'redis:latest'
    ports:
      - "6379:6379"
    volumes:
      - ./redis:/var/lib/redis/data
  web:
    image: registry.inkmap.com:5000/devops/ink-api
    command: '/bin/sh docker/init_tests.sh'
    links:
      - postgres
      - redis
    volumes:
      - ./:/ink-api
    env_file:
      - '.env'
    ports:
      - "3000:3000"
    depends_on:
      - postgres
